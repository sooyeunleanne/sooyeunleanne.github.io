import { ShadowfollowDirective } from './shadowfollow.directive';

describe('ShadowfollowDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowfollowDirective();
    expect(directive).toBeTruthy();
  });
});
